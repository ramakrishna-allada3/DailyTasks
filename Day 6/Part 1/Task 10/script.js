let login = 'Employee';
let message = (login == 'Employee') ? "Welcome Employee" : (login == 'Director') ? 'Greetings' : (login == '') ? 'No login' : '';
console.log(message);

// Explaination: I fixed the above code to welcome the employee.