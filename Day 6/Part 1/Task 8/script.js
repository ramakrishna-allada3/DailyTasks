let a = +prompt("Enter a number?");
//Don't modify any code below this
if (a) {
 console.log( 'OMG it works for any number inc 0' );
}
else
{
 console.log( "Success" );
}

// Explaination: In the above code parsing the input value solves the code to get success. Because integer 0 is false.