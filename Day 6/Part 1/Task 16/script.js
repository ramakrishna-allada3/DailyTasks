//You are allowed to modify only one character 
for (let num = 2; num <= 20; num += 2) {
    console.log(num)
}

// Explaination: As per the requirement, only one character is changed in the above code. To print even numbers, the variable num is incremented by 2 instead of incrementing it with 1.