let a = +prompt("First number?");
let b = +prompt("Second number?");
alert(a + b);

// Explaination: syntax for parsing the string to integer is missing in the actual given code. It is fixed in the above code.