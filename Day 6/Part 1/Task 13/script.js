let message;
let lock = 0;
//Dont change any code below this
if (lock && " " || undefined )
{
  message = "Go away";
}
else
{
 message = "welcome";
}
console.log(message);

// Explaination: I changed the lock variable value to 0 to make the code go to line number 10 and welcome the boss.
