var lemein = "0";
var lemeout = 0;
var msg = "";
if (lemein) {
 msg += "hi";
 }if (lemeout) {
 msg += 'Hello';
}console.log(msg);

// Explaination: lemein is a string 0 and it's boolean is true. So, msg will be hi, and lemeout is int 0 it's boolean value is false. At the end msg will be hi.
